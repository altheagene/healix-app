import type { Config } from "@react-router/dev/config";

export default {
  // Config options...
  // Server-side render by default, to enable SPA mode set this to `false`
  ssr: false,
} satisfies Config;

// import type { Config } from "@react-router/dev/config";

// export default {
//   ssr: false,       // SPA mode
//   router: {
//     type: "hash",   // Use hash routing
//   },
// } satisfies Config;

